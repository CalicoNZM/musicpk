services:
  - type: web
    name: musicpk
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
    autoDeploy: false

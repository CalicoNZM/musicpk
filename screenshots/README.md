# screenshots

grabbed these on my macbook while testing settings — not a mock, actual running site at http://localhost:3000

- genres board with 32 niches
- artist wiki (Bjork page) with edit history
- settings modal with saveable themes

to regenerate: `python3 py_server.py`, open http://localhost:3000, screenshot. no figma.

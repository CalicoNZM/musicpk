name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build check
        run: docker build -t musicpk:test .
      - name: Python smoke
        run: python3 -m py_compile py_server.py
